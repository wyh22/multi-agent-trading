# v1.4 Validation

Validation performed in the build container:

- `python -m compileall -q tradingagents service scripts`: PASS
- `pyproject.toml` TOML parse/version check: PASS (`1.4.0`)
- `docker-compose.yml` YAML/service/volume structure check: PASS
- `pytest -q`: **55 passed, 1 skipped**

New regression coverage includes:

- conversation thread persistence and reset;
- ticker normalization and chat routing;
- `/chat` + history/reset + browser UI routes;
- iFinD code/indicator payload normalization;
- server-side refresh-token isolation in the iFinD HTTP adapter;
- optional iFinD MCP tool exposure;
- external MCP tool allowlist behavior.

Not executed in this environment:

- live LLM chat invocation (requires the user's configured model/API key and optional LangChain runtime extras);
- live iFinD request (requires the user's iFinD refresh token/entitlement);
- live external third-party MCP server invocation;
- full Docker image pull/start against external registries.

These are runtime integration checks to run on the target machine after credentials/services are configured.
